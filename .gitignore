# Match runner — fetched & built by ./setup-fastchess.sh
/fastchess/

# Tournament output
/results/

# fastchess autosave / resume state (machine-specific paths + run stats)
/config.json

# Bots (binaries AND configs) — not committed; fetched by the bootstrap script
/bots/

# chess-eval-scaling engine — cloned & built in-tree by ./bootstrap.sh
# (skipped when an existing build is reused, e.g. a sibling checkout)
/chess-eval-scaling/
